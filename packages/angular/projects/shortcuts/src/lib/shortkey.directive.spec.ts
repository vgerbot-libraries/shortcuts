import { ShortkeyDirective } from './shortcuts-key.directive';

describe('ShortcutsKeyDirective', () => {
    it('should create an instance', () => {
        const directive = new ShortkeyDirective();
        expect(directive).toBeTruthy();
    });
});
